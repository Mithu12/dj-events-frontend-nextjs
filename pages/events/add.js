import Layout from '../../components/Layout';

const Add = (props) => {
    return (
        <Layout title={'Add New Event'}>
            add events page
        </Layout>
    )
};

export default Add;